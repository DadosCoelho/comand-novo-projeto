See the request below and run the verification requested:

$ARGUMENTS

Use the available tools needed, such MCPs, CLIs, and other if needed. Usually you have all available necessary tools to complete the test.

Example, you may have:

- Playwright MCP to test web pages
- CLIs available on this machine (e.g. `git`, `gh`, plus whatever your stack uses)
- Project MCP servers

If there is any task that you really cannot execute because it needs human interaction — either (1) approval or (2) a real-world action — let me know

See file @../../ai-docs/lessons.md to understand the learned lessons during the development that you can use